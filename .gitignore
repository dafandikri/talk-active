.DS_Store
node_modules/
coverage/
/public/
/.tmp-public-*/
/tmp/

# Local source captures are preserved outside Git; the searchable transcription
# and its enforced requirements live in docs/TECHNICAL-MEETING-2026.md.
/docs/Technical Meeting Slides/

# Vercel project linkage is per-developer.
.vercel/

# Secrets. The negation must come AFTER the pattern it negates, or it does
# nothing — which is what happened when `.env*` was appended below
# `!.env.example`.
.env*
!.env.example
