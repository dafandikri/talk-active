# Global Memory (apply to your user-level agent config)

> Copy the parts you want into your machine-global instruction file
> (`~/.claude/CLAUDE.md`, `~/.codex/AGENTS.md`, etc.). These are preferences that should
> hold across *every* project. Keep them short — global memory is loaded into every
> session, so every line costs context.

## Identity & preferences

- The captain is building university hackathon products and values candid differentiation analysis, not automatic encouragement.
- Communication style: concise, actionable, and explicit about what is evidence versus assumption.
- Prefer a clickable first draft that can be discussed with teammates over a large speculative feature list.
- Complexity in sizes (S/M/L/XL), except when an external event has a fixed schedule.

## Non-negotiables

- Never use `any` in TypeScript; never skip type hints in Python.
- Prefer integration tests over unit tests.
- Prefer editing existing files over creating new ones.
- Never auto-commit / auto-push / auto-PR without an explicit request.

## Package managers

- JavaScript/TypeScript: pnpm. Python: uv.
